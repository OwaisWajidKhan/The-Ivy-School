{
  "name": "the-ivy-school",
  "version": "1.0.0",
  "private": true,
  "description": "The Ivy School attendance system — monorepo root. Each service builds itself via vercel.json services (frontend: Vite, backend: Express).",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "backend:install": "npm --prefix backend install",
    "build:frontend": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "engines": {
    "node": ">=22"
  }
}
